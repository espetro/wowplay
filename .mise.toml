[tools]
rust = "1.96"   # current stable; see packages/rust-core/Cargo.toml for edition
zig = "0.16.0"  # used to cross-compile winerosetta.dll (x86-windows-gnu)

[env]
# Ensure Zig is on PATH for scripts/setup.sh and scripts/pre-commit.sh
PATH = "{{env.HOME}}/.local/share/mise/shims:{{env.PATH}}"
