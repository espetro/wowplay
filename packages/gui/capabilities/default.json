{
  "identifier": "default",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-confirm",
    "shell:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "binaries/wowplay", "sidecar": true, "args": true }]
    }
  ]
}
