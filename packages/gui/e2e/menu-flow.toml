name = "menu-flow"

[[steps]]
action = "click"
selector = "[data-testid='options-menu-btn']"

[[steps]]
action = "assert"
selector = "[data-testid='menu-item-toggle-alerts']"
args = { visible = true }

[[steps]]
action = "click"
selector = "[data-testid='menu-item-toggle-alerts']"

[[steps]]
action = "click"
selector = "[data-testid='options-menu-btn']"

[[steps]]
action = "click"
selector = "[data-testid='menu-item-reset']"

[[steps]]
action = "assert"
selector = "[data-testid='runner-select-trigger']"
args = { text = "Select runner..." }
