// This file has been generated by Tauri Specta. Do not edit this file manually.

import { invoke as __TAURI_INVOKE } from "@tauri-apps/api/core"

/** Commands */
export const commands = {
  /**  Gets the current application configuration. */
  getConfig: () =>
    typedError<AppConfig, CommandError>(__TAURI_INVOKE("get_config")),
  /**  Sets the application configuration and persists it to disk. */
  setConfig: (config: AppConfig) =>
    typedError<null, CommandError>(__TAURI_INVOKE("set_config", { config })),
  /**  Resets the application configuration to defaults and clears the store. */
  resetConfig: () =>
    typedError<null, CommandError>(__TAURI_INVOKE("reset_config")),
  /**  Checks all known runners and returns their availability status. */
  checkRunners: () =>
    typedError<RunnerStatus[], CommandError>(__TAURI_INVOKE("check_runners")),
  /**  Runs the one-time setup sequence directly via rust-core. */
  runSetup: (wowDir: string, runner: string) =>
    typedError<SetupResult, CommandError>(
      __TAURI_INVOKE("run_setup", { wowDir, runner }),
    ),
  /**  Validates a WoW installation directory. */
  validateWowDir: (path: string) =>
    typedError<ValidationResult, CommandError>(
      __TAURI_INVOKE("validate_wow_dir", { path }),
    ),
  /**  Launches WoW directly via rust-core and returns the process PID. */
  launchWow: (wowDir: string, runner: string, bottle: string) =>
    typedError<number, CommandError>(
      __TAURI_INVOKE("launch_wow", { wowDir, runner, bottle }),
    ),
}

/* Types */
/**  Persisted application configuration. */
export type AppConfig = {
  /**  Selected runner name (e.g. "crossover", "whisky", "moonshine"). */
  runner: string | null
  /**  Path to the WoW 3.3.5a game directory. */
  wow_dir: string | null
  /**  Whether to show info-level alerts in the UI. */
  show_alerts?: boolean
  /**  Wine/CrossOver bottle name (e.g. "Win10"). */
  bottle?: string | null
}

/**  A serializable error for Tauri commands. */
export type CommandError = {
  message: string
}

/**  Status of a runner for the frontend dropdown. */
export type RunnerStatus = {
  name: string
  display_name: string
  available: boolean
  path: string | null
}

/**  Result of running the setup sequence. */
export type SetupResult = {
  success: boolean
  messages: string[]
}

/**  Result of validating a WoW directory. */
export type ValidationResult = {
  valid: boolean
  wow_exe_found: boolean
  divxdecoder_patched: boolean
  message: string
  severity: string
}

/* Tauri Specta runtime */
async function typedError<T, E>(
  result: Promise<T>,
): Promise<{ status: "ok"; data: T } | { status: "error"; error: E }> {
  try {
    return { status: "ok", data: await result }
  } catch (e) {
    if (e instanceof Error) throw e
    return { status: "error", error: e as any }
  }
}
